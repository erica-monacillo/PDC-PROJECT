version: '3.8'

services:
  pdc-app:
    build: .
    container_name: pdc-project-lb
    ports:
      - "80:80"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped